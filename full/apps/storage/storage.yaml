apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: app-storage-pvc
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
---
apiVersion: batch/v1
kind: Job
metadata:
  name: init-storage
spec:
  template:
    spec:
      containers:
      - name: init-container
        image: alpine/git
        command: ["/bin/sh", "-c"]
        args:
        - |
          git clone https://github.com/jayzaa/Lab-Git/full/disk /mnt/storage &&
          cp -r /mnt/storage/* /mnt/data
        volumeMounts:
        - name: app-storage-volume
          mountPath: /mnt/data
      restartPolicy: OnFailure
      volumes:
      - name: app-storage-volume
        persistentVolumeClaim:
          claimName: app-storage-pvc
